google-api-python-client>=2.100.0
google-auth>=2.20.0
faker>=20.0.0
google-genai>=1.73.0
# Requis par google-adk[a2a] pour AgentRegistry.get_mcp_toolset()
a2a-sdk>=0.3.26
google-cloud-iamconnectorcredentials>=0.1.0
# Tests d'intégration (Testcontainers — nécessite Docker)
testcontainers[postgres,redis]>=4.0.0
# Driver PostgreSQL synchrone pour les fixtures wipe_db() des conftest (DDL setup/teardown)
# Les services utilisent asyncpg (async) mais SQLAlchemy create_engine() requiert psycopg2
psycopg2-binary>=2.9.0
# Build et publication du wheel zenika-shared-schemas (utilisés par deploy.sh)
build>=1.0.0
twine>=5.0.0
