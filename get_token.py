import os
import requests

# We need the users api url (the load balancer one)
url = "https://dev.zenika.slavayssiere.fr/auth/login"
# Assuming there is a default test user or we can find it in seed_data.py
